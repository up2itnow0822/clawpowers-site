import { Card, SiteShell } from "../components";

export default function Page() {
  return (
    <SiteShell eyebrow="clawpowers.ai" title="Examples">
      <Card>
        <p className="text-base leading-7 text-slate-600">Examples gallery for install, payment flows, memory retrieval, RSI loops, wallet operations, and framework integrations.</p>
      </Card>
    </SiteShell>
  );
}
